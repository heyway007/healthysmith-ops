// Thai name prefixes -- shared between the HR module (admin) and the
// employee self-service profile (portal) so both offer the same fixed list.
export const PREFIX_NAMES = ["นาย", "นาง", "นางสาว"] as const;
